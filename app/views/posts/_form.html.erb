<%= simple_form_for @post do |f| %>
  <%= f.input :blog_id, collection: Blog.all.pluck(:id, :name), label_method: :second, value_method: :first %>
  <%= f.input :name %>
  <%= f.button :submit, class: "btn btn-primary" %>
  <%= link_to 'Back', posts_path, class: "btn btn-danger" %>
<% end %>
